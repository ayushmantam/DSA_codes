//Transform into sum tree

class Solution{
    public:

    int sum(Node* root){
        if(!root) return 0;

        if(!root->left && !root->right){
            //leaf Node
            int temp=root->data;

            return temp;
        }
        int num
    }
};